# FLISR Edge & Network Simulation Platform

Project space to version control the FLISR Edge dissertation document.

## Building the document

The document is written in LaTex and built using latexmk.

To build the template, navigate to the `src` folder and use the following command:

> latexmk -pdf
